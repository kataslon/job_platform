<h2>All companies</h2>
<div class="row">
  <div class="span12">
    <div align='right'>
      <% if can? :new, Company %>
        <%= link_to "New company", new_company_path %>
      <% end %>
    </div>
    <%= render "grid" %>
  </div>
</div>
